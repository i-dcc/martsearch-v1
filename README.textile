h1. MartSearch

MartSearch is a configurable web services based search interface for Biomart. The idea is simple, take the sometimes complicated search interface of MartView, and replace it with a single free text search box. How this is achieved is not so simple however…